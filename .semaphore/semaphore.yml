version: v1.0
name: First Apple Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: Hello MacOS
    task:
      env_vars:
        - name: Test
          value: hello env vars
      prologue:
        commands:
          - checkout
      jobs:
        - name: Hello1
          commands:
            - echo "bash asdsdasd" > script.sh && chmod +x script.sh
            - bash script.sh
