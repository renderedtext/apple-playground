version: v1.0
name: First Apple Pipeline
agent:
  machine:
    type: a1-standard-4
    os_image: macos-mojave

blocks:
  - name: "Hello MacOS"
    task:
      env_vars:
        - name: Test
          value: "hello env vars"

      prologue:
        commands:
          - checkout
          - pwd

      jobs:
      - name: Hello1
        commands:
          - stty -a
          - brew install go
          - stty -a
          - echo hello
          - cd /tmp
          - ls
