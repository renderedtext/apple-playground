version: v1.0
name: First Apple Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: osx-mojave

blocks:
  - name: "Hello"
    task:
      env_vars:
        - name: Test
          value: "hello env vars"

      prologue:
        commands:
          - pwd
          - ls -lah
          - chmod 0600 ~/.ssh/id_rsa
          - checkout

      jobs:
      - name: Hello
        commands:
          - echo hello
      - name: Hello
        commands:
          - echo hello
      - name: Hello
        commands:
          - echo hello
      - name: Hello
        commands:
          - echo hello
