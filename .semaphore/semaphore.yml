version: v1.0
name: First Apple Pipeline
agent:
  machine:
    type: a1-standard-4
    os_image: macos-mojave-xcode11

blocks:
  - name: "Hello MacOS"
    task:
      env_vars:
        - name: Test
          value: "hello env vars"

      prologue:
        commands:
          - checkout

      jobs:
      - name: Hello1
        commands:
          - xcversion installed
